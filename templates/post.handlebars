<!DOCTYPE html>
<html lang="nl">
<head>
  {{> head }}
<body>

    {{> header}}

  <aside>
      {{> nav }}
  </aside>

  
 <main>
    <article>
      <section class="small-container blog_header">
        {{#if body.thumbnail }}
          <img class="thumb" src="../../img/{{custom.thumbnail}}" />
        {{/if}}
        <h1>{{title}}</h1>
        {{!-- <span class="date">{{format_date body.creation_date}}</span> --}}
      </section>
      <section class="small-container">{{{ extract_images content }}}</section> 
    </article> 

    {{> tabs}}

</main>

    {{> footer }}
</body>
</html> 